package Java;

public class PackInheritance 
{
	//Protected: accessible within the package and outside the package BUT through INHERITANCE
	//PRIVATE: only within the same class
	//DEFAULT:  available only within the class and package(not accessible outside the package)
	protected int j=5;
	protected void testing()

{
	System.out.println(j);
}
}
